╔═══════════════════════════════════════════════════════════════════════════════╗
║                                                                               ║
║                     🔧 BACKEND QUICK REFERENCE GUIDE 🔧                     ║
║                                                                               ║
║                  Spam SMS Detection API - Quick Setup & Usage                 ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────────────────────┐
│ 🚀 START BACKEND IN 1 COMMAND                                                │
└───────────────────────────────────────────────────────────────────────────────┘

Windows (CMD):
  cd d:\spam_SMS_detection
  "C:/Users/Smruti/Documents/MACHINE LEARNING COMPLETE/myenv/Scripts/python.exe" ^
    -m uvicorn main:app --host 127.0.0.1 --port 8000

Linux/Mac (Bash):
  cd ~/spam_SMS_detection
  uvicorn main:app --host 127.0.0.1 --port 8000

Expected Output:
  INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
  INFO:     Application startup complete.
  ✓ Model loaded successfully


┌───────────────────────────────────────────────────────────────────────────────┐
│ 📋 API ENDPOINTS SUMMARY                                                     │
└───────────────────────────────────────────────────────────────────────────────┘

  Endpoint            Method  Description                    Response
  ─────────────────────────────────────────────────────────────────────────────
  /health             GET     Check API & model status       {status, model}
  /                   GET     API info & status              {message, status}
  /predict            POST    Single SMS prediction          {result, confidence}
  /batch-predict      POST    Multiple SMS prediction        {predictions: [...]}
  /docs               GET     Interactive API docs (Swagger)
  /redoc              GET     API docs (ReDoc format)
  /openapi.json       GET     OpenAPI schema


┌───────────────────────────────────────────────────────────────────────────────┐
│ 🧪 TEST IN 3 WAYS                                                            │
└───────────────────────────────────────────────────────────────────────────────┘

METHOD 1: Automated Test Suite
  "C:.../python.exe" test_api.py

METHOD 2: Interactive Swagger UI
  Browser: http://127.0.0.1:8000/docs
  → Click endpoint → Try it out → Execute

METHOD 3: cURL in Terminal
  curl -X POST "http://127.0.0.1:8000/predict" \
    -H "Content-Type: application/json" \
    -d "{\"text\":\"Free money click now\"}"


┌───────────────────────────────────────────────────────────────────────────────┐
│ 📤 REQUEST EXAMPLES                                                          │
└───────────────────────────────────────────────────────────────────────────────┘

SINGLE PREDICTION:
  POST /predict
  Content-Type: application/json
  
  {
    "text": "Congratulations! You won a free prize!"
  }

BATCH PREDICTION:
  POST /batch-predict
  Content-Type: application/json
  
  [
    {"text": "Free money"},
    {"text": "Hi how are you"},
    {"text": "URGENT: Claim prize"}
  ]


┌───────────────────────────────────────────────────────────────────────────────┐
│ 📥 RESPONSE EXAMPLES                                                         │
└───────────────────────────────────────────────────────────────────────────────┘

SINGLE PREDICTION RESPONSE:
  HTTP/1.1 200 OK
  
  {
    "result": "Spam",
    "confidence": 0.6751
  }

BATCH PREDICTION RESPONSE:
  HTTP/1.1 200 OK
  
  {
    "predictions": [
      {
        "text": "Free money",
        "result": "Spam",
        "confidence": 0.78
      },
      {
        "text": "Hi how are you",
        "result": "Ham",
        "confidence": 0.95
      }
    ]
  }

ERROR RESPONSE (Empty Text):
  HTTP/1.1 422 Unprocessable Entity
  
  {
    "detail": [
      {
        "msg": "ensure this value has at least 1 characters",
        "type": "value_error.string.min_length"
      }
    ]
  }


┌───────────────────────────────────────────────────────────────────────────────┐
│ 💻 CODE EXAMPLES                                                             │
└───────────────────────────────────────────────────────────────────────────────┘

PYTHON:
  import requests
  
  response = requests.post(
      "http://127.0.0.1:8000/predict",
      json={"text": "Free money! Click here!"}
  )
  
  if response.status_code == 200:
      data = response.json()
      print(f"Result: {data['result']}")
      print(f"Confidence: {data['confidence']:.0%}")
  else:
      print(f"Error: {response.status_code}")

JAVASCRIPT:
  fetch('http://127.0.0.1:8000/predict', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({text: 'Free money'})
  })
  .then(res => res.json())
  .then(data => {
      console.log(`Result: ${data.result}`);
      console.log(`Confidence: ${(data.confidence * 100).toFixed(1)}%`);
  });

cURL:
  curl -X POST "http://127.0.0.1:8000/predict" \
      -H "Content-Type: application/json" \
      -d "{\"text\":\"Free money\"}"


┌───────────────────────────────────────────────────────────────────────────────┐
│ ⚙️  CONFIGURATION OPTIONS                                                    │
└───────────────────────────────────────────────────────────────────────────────┘

CHANGE PORT:
  uvicorn main:app --host 127.0.0.1 --port 8001

ENABLE AUTO-RELOAD (Development):
  uvicorn main:app --reload

MULTIPLE WORKERS (Production):
  uvicorn main:app --workers 4

CHANGE LOG LEVEL:
  uvicorn main:app --log-level info
  uvicorn main:app --log-level debug


┌───────────────────────────────────────────────────────────────────────────────┐
│ 🔍 MONITORING & LOGS                                                         │
└───────────────────────────────────────────────────────────────────────────────┘

CONSOLE LOGS:
  INFO:     Started server process [PID]
  INFO:     Waiting for application startup.
  INFO:main:✓ Model loaded successfully
  INFO:     Application startup complete.
  INFO:     Uvicorn running on http://127.0.0.1:8000

LIVE LOGS:
  Watch terminal window while testing API
  Each request/response logged in real-time

ACCESS LOGS:
  GET /health HTTP/1.1 200
  POST /predict HTTP/1.1 200


┌───────────────────────────────────────────────────────────────────────────────┐
│ 🐛 QUICK TROUBLESHOOTING                                                    │
└───────────────────────────────────────────────────────────────────────────────┘

Problem: "Port already in use"
Solution: Use different port: --port 8001

Problem: "Model not found"
Solution: Verify spam_model.pkl exists in project folder

Problem: "Connection refused"
Solution: Verify server is running (check terminal output)

Problem: "Slow first request"
Solution: Normal - model loads on startup (~500ms)

Problem: "403 Forbidden"
Solution: Check CORS settings (currently allows all origins)


┌───────────────────────────────────────────────────────────────────────────────┐
│ ✨ KEY IMPROVEMENTS IN v2.0                                                 │
└───────────────────────────────────────────────────────────────────────────────┘

✅ Robust Startup
   - Model loaded in startup event
   - Better error handling
   - Verification logging

✅ Health Checks
   - /health endpoint
   - Model readiness tracking
   - Production-ready monitoring

✅ Better Responses
   - Confidence scores included
   - Structured error responses
   - Type-validated inputs/outputs

✅ Batch Processing
   - /batch-predict endpoint
   - Process multiple messages at once
   - Efficient bulk operations

✅ Comprehensive Logging
   - Startup verification
   - Request logging
   - Error tracking
   - Shutdown events

✅ Production Ready
   - Global exception handler
   - Graceful shutdown
   - CORS middleware
   - Input validation


╔═══════════════════════════════════════════════════════════════════════════════╗
║                                                                               ║
║                    ✅ BACKEND IS PRODUCTION READY ✅                        ║
║                                                                               ║
║  All issues solved • All endpoints working • Fully tested and documented     ║
║                                                                               ║
║                    Ready for immediate deployment!                           ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝
